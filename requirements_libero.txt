# LIBERO-Goal environment (encoder pretraining + DiTFlow/VQ-BeT BC policy for LIBERO-Goal).
# Pinned to the versions used for the released results. The LIBERO env code is vendored under
# libero_goal/envs/ (CANON-modified spherical multi-view camera); the sim runs on robosuite.
torch==2.0.1
torchvision==0.15.2
numpy==1.24.4
einops==0.6.1
h5py==3.9.0
scipy==1.10.1
tqdm
wandb==0.26.1
hydra-core==1.3.2
omegaconf==2.3.0
accelerate==0.22.0
transformers==4.46.3
easydict
matplotlib==3.7.2
imageio==2.31.1
imageio-ffmpeg==0.4.8
opencv-python==4.8.0.76
# Simulator (LIBERO runs on robosuite + bddl; assets are bundled under libero_goal/envs/assets)
robosuite==1.4.1
bddl==3.5.0
robomimic==0.3.0
mujoco==2.3.7
gym==0.23.1
